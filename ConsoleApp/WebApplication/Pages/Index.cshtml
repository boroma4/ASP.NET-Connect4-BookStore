@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Connect4</h1>
    <p>Let's play!</p>
    <button type="submit" class="btn btn-outline-info btn-lg">
         <a asp-page="./PlayOnline"> @Model.Start</a>
    </button>
        <button type="submit" class="btn btn-outline-info btn-lg">
             <a asp-page="./Load"> @Model.Load</a>
        </button>
</div>
